package TestUnitTaker
//import ClosureTimers
import RegisterEvents

init
    registerPlayerUnitEvent(EVENT_PLAYER_UNIT_ATTACKED) ->
        let u = GetAttacker()
        if u.getTypeId() == 'H05K'
            u.issuePointOrderById(852125, vec2(GetTriggerUnit().getX(), GetTriggerUnit().getY()))
